@model IEnumerable<ERP6.Models.Acccv10>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Subno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Subno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Subno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
